name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13"]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install dependencies
        run: python -m pip install -r requirements-dev.txt

      - name: Run tests
        run: python -m pytest -q

      - name: Generate evaluation dataset
        run: |
          python data/synthetic_generator.py \
            --customers 120 \
            --months 3 \
            --seed 7 \
            --out /tmp/fincrime-evaluation

      - name: Run ground-truth evaluation
        run: |
          python evaluation/evaluate.py \
            --transactions /tmp/fincrime-evaluation/transactions.csv \
            --ground-truth /tmp/fincrime-evaluation/ground_truth.csv \
            --thresholds evaluation/thresholds.json \
            --out /tmp/evaluation-results.json
