# Fold rtm-model-tuning-dashboard into bear-fincrime-toolkit-2.0 Implementation Plan

> For agentic workers: REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (- [ ]) syntax for tracking.

Goal: Merge overjoyde/rtm-model-tuning-dashboard's full history into overjoyde/bear-fincrime-toolkit-2.0 under a new dashboard/ folder, give it a first real view (live score-threshold tuning against the toolkit's own detectors and ground truth), add scoped CI for it, cross-link the root README, and open a draft PR.

Architecture: git subtree add brings the dashboard's scaffold history in under a path prefix. A new Python script regenerates a checked-in JSON snapshot from the toolkit's existing synthetic-data generator, detection pipeline, and ground truth. The React app reads that static snapshot (no backend) and lets the user drag a per-detector score threshold, recomputing precision/recall/F1 live in the browser via a small pure-function module. A hand-rolled inline-SVG chart visualizes precision/recall across the threshold range with the configured floor as a reference line.

Tech Stack: git (subtree), Python 3.11+/pandas (snapshot generation, reusing existing toolkit modules), React 18 + Vite 5 (existing dashboard scaffold), plain JS (no new npm dependency), GitHub Actions YAML, Markdown.

## Global Constraints

- Side-by-side integration only, plus this one real view - no changes to any detector's internal logic (pipelines/detectors/*.py) or its structural thresholds.
- Dashboard's git history preserved commit-by-commit via subtree, not squashed.
- Target folder name is exactly dashboard/.
- No new npm dependency for the chart - a hand-rolled inline SVG only.
- Data flow is a checked-in static sample snapshot (dashboard/src/data/tuning-snapshot.json), regenerated by a documented manual script - never part of CI or the build.
- No test-runner infrastructure added (no Vitest, no node:test wiring into package.json) - this was an explicit decision in the approved spec. Any verification of dashboard/src/lib/metrics.js must be done with a throwaway script that is run and then deleted, never committed.
- New CI workflow (dashboard-ci.yml) and the existing ci.yml both get paths: filters so each only runs on its own project's changes.
- No line in the root README changes except: one new line in the "Repository layout" tree, and one existing connector character changed from a corner to a middle-branch to accommodate it.
- No dependency unification between the toolkit's pip/requirements-dev.txt and the dashboard's npm/package.json.
- Land via a PR against main on branch add-rtm-dashboard, draft first.
- All work happens in the existing local clone at the plan-runner's cwd (~/Projects/bear-fincrime-toolkit-2.0), already on branch add-rtm-dashboard (created off main), with the design spec already committed as f431659.

---

### Task 1: Subtree-merge rtm-model-tuning-dashboard into dashboard/

Files:
- Create: everything under dashboard/ (package.json, package-lock.json, vite.config.js, index.html, src/App.jsx, src/main.jsx, README.md, LICENSE, .gitignore)
- Modify: none

Interfaces:
- Consumes: nothing from earlier tasks (first task).
- Produces: dashboard/ directory tree on disk, on branch add-rtm-dashboard, for every later task to build on. In particular, dashboard/src/App.jsx and dashboard/src/main.jsx exist at these exact paths for Task 4 to modify.

- [ ] Step 1: Confirm starting state

Run: git status && git log --oneline -3
Expected: On branch add-rtm-dashboard, clean working tree, top commit is f431659 Add design spec for folding rtm-model-tuning-dashboard into dashboard/.

- [ ] Step 2: Run the subtree merge

Run:
git subtree add --prefix=dashboard https://github.com/overjoyde/rtm-model-tuning-dashboard.git main -m "Merge rtm-model-tuning-dashboard into dashboard/"

Expected: git prints fetch progress for the remote, then "Added dir 'dashboard'". The merge commit is created automatically by this command - do not run a separate git commit for this step.

- [ ] Step 3: Verify the merge

Run: git log --oneline -5 && ls -a dashboard && ls dashboard/src
Expected: top commit message is "Merge rtm-model-tuning-dashboard into dashboard/"; dashboard listing includes package.json package-lock.json vite.config.js index.html README.md LICENSE .gitignore src; dashboard/src listing includes App.jsx main.jsx.

- [ ] Step 4: Verify nothing outside dashboard/ changed

Run: git diff f431659 HEAD --stat -- . ':!dashboard'
Expected: empty output.

No separate commit step - git subtree add already created the merge commit in Step 2.

---

### Task 2: Generate the checked-in tuning snapshot

Files:
- Create: dashboard/scripts/export_tuning_snapshot.py
- Create: dashboard/src/data/tuning-snapshot.json (generated by running the script above, then committed)

Interfaces:
- Consumes: pipelines/run_pipeline.run(transactions_path) -> pandas.DataFrame with columns entity_id, detector, reason, score, raw_score (and more); evaluation/evaluate.py's TARGET_ROLES dict (keys: structuring, fan_in_out, pass_through, community_ring; values: sets of role strings); evaluation/thresholds.json (keys: structuring, fan_in_out, pass_through, each a dict with minimum_precision and minimum_recall floats; no community_ring key); data/synthetic_generator.py's CLI (--customers, --months, --seed, --out DIR, writing DIR/transactions.csv and DIR/ground_truth.csv with columns entity_id, typology, role for the ground truth file).
- Produces: dashboard/src/data/tuning-snapshot.json with this exact shape, consumed by Task 3 (metrics.js) and Task 4 (App.jsx's JSON import):

    {
      "<detector>": {
        "flagged": [{"entity_id": "<str>", "score": <float>}],
        "ground_truth_positive_ids": ["<str>"],
        "ground_truth_positive_count": <int>,
        "floor": {"minimum_precision": <float>, "minimum_recall": <float>} (or null)
      }
    }

  One top-level key per TARGET_ROLES key (structuring, fan_in_out, pass_through, community_ring). community_ring's floor is null since thresholds.json has no entry for it.

- [ ] Step 1: Write dashboard/scripts/export_tuning_snapshot.py

Write this exact content (note: the docstring below uses triple-double-quotes, standard Python syntax):

    #!/usr/bin/env python3
    """Generate the dashboard's checked-in tuning snapshot from the toolkit's
    own synthetic data generator, detection pipeline, and ground truth.

    Regenerate after changing detector logic or the synthetic generator:
        python dashboard/scripts/export_tuning_snapshot.py
    """
    import json
    import subprocess
    import sys
    import tempfile
    from pathlib import Path

    import pandas as pd

    ROOT = Path(__file__).resolve().parents[2]
    if str(ROOT) not in sys.path:
        sys.path.insert(0, str(ROOT))

    from pipelines.run_pipeline import run  # noqa: E402
    from evaluation.evaluate import TARGET_ROLES  # noqa: E402

    OUT_PATH = Path(__file__).resolve().parents[1] / "src" / "data" / "tuning-snapshot.json"
    THRESHOLDS_PATH = ROOT / "evaluation" / "thresholds.json"


    def generate_data(out_dir):
        subprocess.run(
            [
                sys.executable,
                str(ROOT / "data" / "synthetic_generator.py"),
                "--customers", "120",
                "--months", "3",
                "--seed", "7",
                "--out", str(out_dir),
            ],
            check=True,
        )
        return out_dir / "transactions.csv", out_dir / "ground_truth.csv"


    def build_snapshot(flags, ground_truth, thresholds):
        snapshot = {}
        for detector, target_roles in TARGET_ROLES.items():
            detector_flags = flags[flags["detector"] == detector]
            flagged = [
                {"entity_id": str(row.entity_id), "score": float(row.score)}
                for row in detector_flags.itertuples()
            ]
            target_rows = ground_truth[
                (ground_truth["typology"] == detector)
                & (ground_truth["role"].isin(target_roles))
            ]
            ground_truth_positive_ids = sorted(
                target_rows["entity_id"].astype(str).unique().tolist()
            )
            snapshot[detector] = {
                "flagged": flagged,
                "ground_truth_positive_ids": ground_truth_positive_ids,
                "ground_truth_positive_count": len(ground_truth_positive_ids),
                "floor": thresholds.get(detector),
            }
        return snapshot


    def main():
        with tempfile.TemporaryDirectory() as tmp:
            transactions_path, ground_truth_path = generate_data(Path(tmp))
            flags = run(transactions_path)
            ground_truth = pd.read_csv(ground_truth_path)

        with THRESHOLDS_PATH.open(encoding="utf-8") as handle:
            thresholds = json.load(handle)

        snapshot = build_snapshot(flags, ground_truth, thresholds)

        OUT_PATH.parent.mkdir(parents=True, exist_ok=True)
        OUT_PATH.write_text(json.dumps(snapshot, indent=2) + "\n", encoding="utf-8")
        print("Wrote snapshot for " + str(len(snapshot)) + " detectors to " + str(OUT_PATH))


    if __name__ == "__main__":
        main()

- [ ] Step 2: Run it and inspect the output

Run: python dashboard/scripts/export_tuning_snapshot.py
Expected: prints "Wrote snapshot for 4 detectors to .../dashboard/src/data/tuning-snapshot.json"; the file exists.

- [ ] Step 3: Verify the generated JSON's shape and values

Run this one-line check (adjust quoting for your shell if needed):
python -c "import json; d = json.load(open('dashboard/src/data/tuning-snapshot.json')); print(sorted(d.keys())); print({k: (len(v['flagged']), v['ground_truth_positive_count'], v['floor']) for k, v in d.items()})"

Expected: keys are exactly ['community_ring', 'fan_in_out', 'pass_through', 'structuring']. community_ring's floor prints as None; structuring, fan_in_out, and pass_through's floors print as their minimum_precision/minimum_recall dicts (matching evaluation/thresholds.json's values). structuring, fan_in_out, and pass_through should each have at least one flagged entity (len(flagged) greater than 0) - the toolkit's own evaluation run on this exact seed/customer/month combination previously showed 100%/100%/80%-plus precision/recall for these three, so their flagged lists must be non-empty. If any of the three come back empty, stop and report BLOCKED - that would mean the snapshot isn't usable for tuning.

- [ ] Step 4: Commit

Run:
git add dashboard/scripts/export_tuning_snapshot.py dashboard/src/data/tuning-snapshot.json
git commit -m "Add tuning-snapshot export script and generated snapshot"

---

### Task 3: Write metrics.js (pure precision/recall/F1 logic)

Files:
- Create: dashboard/src/lib/metrics.js

Interfaces:
- Consumes: a "detector snapshot" object shaped exactly as one value in Task 2's tuning-snapshot.json: {flagged: [{entity_id, score}], ground_truth_positive_ids: [str], ground_truth_positive_count: int, floor: {...} or null}.
- Produces two named exports for Task 4 to import:
  - computeMetricsAtThreshold(detectorSnapshot, threshold) returns {precision, recall, f1, tp, fp, fn} (all numbers).
  - metricsCurve(detectorSnapshot) returns an array of {threshold, precision, recall, f1, tp, fp, fn}, one entry per distinct score value present in detectorSnapshot.flagged, sorted ascending by threshold.

- [ ] Step 1: Write dashboard/src/lib/metrics.js

Write this exact content:

    export function computeMetricsAtThreshold(detectorSnapshot, threshold) {
      const passing = detectorSnapshot.flagged.filter((entity) => entity.score >= threshold)
      const positiveIds = new Set(detectorSnapshot.ground_truth_positive_ids)
      const tp = passing.filter((entity) => positiveIds.has(entity.entity_id)).length
      const fp = passing.length - tp
      const groundTruthCount = detectorSnapshot.ground_truth_positive_count
      const fn = groundTruthCount - tp
      const precision = passing.length > 0 ? tp / passing.length : 0
      const recall = groundTruthCount > 0 ? tp / groundTruthCount : 1
      const f1 = precision + recall > 0 ? (2 * precision * recall) / (precision + recall) : 0
      return { precision, recall, f1, tp, fp, fn }
    }

    export function metricsCurve(detectorSnapshot) {
      const thresholds = Array.from(
        new Set(detectorSnapshot.flagged.map((entity) => entity.score)),
      ).sort((a, b) => a - b)
      return thresholds.map((threshold) => ({
        threshold,
        ...computeMetricsAtThreshold(detectorSnapshot, threshold),
      }))
    }

- [ ] Step 2: Verify with a throwaway script (do not commit this file)

Write a temporary file dashboard/scripts/_verify_metrics.mjs with this content:

    import { computeMetricsAtThreshold, metricsCurve } from '../src/lib/metrics.js'

    const fixture = {
      flagged: [
        { entity_id: 'A', score: 90 },
        { entity_id: 'B', score: 70 },
        { entity_id: 'C', score: 65 },
      ],
      ground_truth_positive_ids: ['A', 'B'],
      ground_truth_positive_count: 2,
      floor: { minimum_precision: 0.5, minimum_recall: 0.5 },
    }

    const at60 = computeMetricsAtThreshold(fixture, 60)
    console.assert(at60.tp === 2 && at60.fp === 1, 'threshold 60: expected tp=2 fp=1, got ' + JSON.stringify(at60))

    const at80 = computeMetricsAtThreshold(fixture, 80)
    console.assert(at80.tp === 1 && at80.fp === 0 && at80.fn === 1, 'threshold 80: expected tp=1 fp=0 fn=1, got ' + JSON.stringify(at80))

    const curve = metricsCurve(fixture)
    console.assert(curve.length === 3, 'expected 3 distinct thresholds, got ' + curve.length)

    console.log('metrics.js verification passed')

Run: node dashboard/scripts/_verify_metrics.mjs
Expected: prints "metrics.js verification passed" with no assertion errors (a failed console.assert prints "Assertion failed:" to stderr but does not stop execution - treat any such output as a failure and fix metrics.js before proceeding).

- [ ] Step 3: Delete the throwaway verification file

Run: rm dashboard/scripts/_verify_metrics.mjs
This file must never be committed - the approved spec explicitly decided against adding test-runner infrastructure to this project.

- [ ] Step 4: Commit

Run:
git add dashboard/src/lib/metrics.js
git commit -m "Add metrics.js: pure precision/recall/F1 computation for tuning"

---

### Task 4: Build the tuning UI (chart, panel, App wiring, dashboard README)

Files:
- Create: dashboard/src/components/PrecisionRecallChart.jsx
- Create: dashboard/src/components/DetectorTuningPanel.jsx
- Modify: dashboard/src/App.jsx (currently: a function component rendering a <main> with an <h1>RTM Model Tuning Dashboard</h1> and a <p>Scaffold only - no views implemented yet.</p> - replace this whole body)
- Modify: dashboard/README.md (currently says "Currently a bare scaffold - no views implemented yet." - replace that line and add a section describing the tuning view)

Interfaces:
- Consumes: computeMetricsAtThreshold and metricsCurve from ../lib/metrics.js (Task 3); the tuning-snapshot.json shape from Task 2, imported directly as a JS module (Vite supports static JSON imports natively).
- Produces: a working dashboard at dashboard/src/App.jsx that Task 7 verifies with npm run build / npm run dev.

- [ ] Step 1: Write dashboard/src/components/PrecisionRecallChart.jsx

Write this exact content:

    const WIDTH = 320
    const HEIGHT = 160
    const PADDING = 24

    function scaleX(threshold) {
      return PADDING + ((threshold - 60) / 40) * (WIDTH - 2 * PADDING)
    }

    function scaleY(value) {
      return HEIGHT - PADDING - value * (HEIGHT - 2 * PADDING)
    }

    function toPoints(curve, key) {
      return curve.map((point) => scaleX(point.threshold) + ',' + scaleY(point[key])).join(' ')
    }

    export default function PrecisionRecallChart({ curve, floor }) {
      return (
        <svg width={WIDTH} height={HEIGHT} role="img" aria-label="Precision and recall versus score threshold">
          <polyline points={toPoints(curve, 'precision')} fill="none" stroke="#2563eb" strokeWidth="2" />
          <polyline points={toPoints(curve, 'recall')} fill="none" stroke="#16a34a" strokeWidth="2" />
          {floor && (
            <line
              x1={PADDING}
              x2={WIDTH - PADDING}
              y1={scaleY(floor.minimum_precision)}
              y2={scaleY(floor.minimum_precision)}
              stroke="#dc2626"
              strokeDasharray="4 4"
            />
          )}
        </svg>
      )
    }

- [ ] Step 2: Write dashboard/src/components/DetectorTuningPanel.jsx

Write this exact content:

    import { useState } from 'react'
    import { computeMetricsAtThreshold, metricsCurve } from '../lib/metrics'
    import PrecisionRecallChart from './PrecisionRecallChart'

    export default function DetectorTuningPanel({ name, snapshot }) {
      const [threshold, setThreshold] = useState(60)
      const metrics = computeMetricsAtThreshold(snapshot, threshold)
      const curve = metricsCurve(snapshot)
      const floor = snapshot.floor
      const passes = floor
        ? metrics.precision >= floor.minimum_precision && metrics.recall >= floor.minimum_recall
        : null

      return (
        <section>
          <h2>{name}</h2>
          <label>
            Score threshold: {threshold}
            <input
              type="range"
              min="60"
              max="100"
              value={threshold}
              onChange={(event) => setThreshold(Number(event.target.value))}
            />
          </label>
          <ul>
            <li>Precision: {(metrics.precision * 100).toFixed(1)}%</li>
            <li>Recall: {(metrics.recall * 100).toFixed(1)}%</li>
            <li>F1: {(metrics.f1 * 100).toFixed(1)}%</li>
            <li>TP: {metrics.tp}, FP: {metrics.fp}, FN: {metrics.fn}</li>
          </ul>
          <p>
            {floor
              ? 'Floor: precision >= ' + (floor.minimum_precision * 100).toFixed(0) + '%, recall >= ' +
                (floor.minimum_recall * 100).toFixed(0) + '% - ' + (passes ? 'PASS' : 'FAIL')
              : 'No floor configured for this detector.'}
          </p>
          <PrecisionRecallChart curve={curve} floor={floor} />
        </section>
      )
    }

- [ ] Step 3: Rewrite dashboard/src/App.jsx

Write this exact content (replacing the entire current file):

    import snapshot from './data/tuning-snapshot.json'
    import DetectorTuningPanel from './components/DetectorTuningPanel'

    function App() {
      return (
        <main>
          <h1>RTM Model Tuning Dashboard</h1>
          <p>
            Drag each detector's score threshold to see precision, recall, and F1
            recompute live against the toolkit's synthetic ground truth.
          </p>
          {Object.entries(snapshot).map(([name, detectorSnapshot]) => (
            <DetectorTuningPanel key={name} name={name} snapshot={detectorSnapshot} />
          ))}
        </main>
      )
    }

    export default App

- [ ] Step 4: Update dashboard/README.md

Replace the line "Currently a bare scaffold - no views implemented yet." with a short paragraph describing the tuning view: what it shows (per-detector score-threshold slider, live precision/recall/F1 against the toolkit's synthetic ground truth, the configured floor as a reference line), and one line noting that dashboard/src/data/tuning-snapshot.json is a checked-in static sample regenerated by running `python dashboard/scripts/export_tuning_snapshot.py` from the repo root. Leave the rest of the README (Getting started, Scripts table, License) unchanged.

- [ ] Step 5: Verify it runs

Run: cd dashboard && npm install && npm run build
Expected: the build completes successfully (exit code 0), producing a dist/ directory with no errors. If you have time, also run `npm run dev` briefly and confirm no console errors on load (this step is optional if a browser isn't available in your environment - the build succeeding is the required check).

- [ ] Step 6: Commit

Run (from the repo root):
git add dashboard/src/components/PrecisionRecallChart.jsx dashboard/src/components/DetectorTuningPanel.jsx dashboard/src/App.jsx dashboard/README.md
git commit -m "Build the tuning view: score-threshold slider, live metrics, chart"

Do not commit dashboard/node_modules or dashboard/dist - check dashboard/.gitignore already excludes them (it should, from the original scaffold) before running git add -A anywhere in this task.

---

### Task 5: Add path-filtered CI for the dashboard, and path-filter the existing ci.yml

Files:
- Create: .github/workflows/dashboard-ci.yml
- Modify: .github/workflows/ci.yml (its `on:` block only)

Interfaces:
- Consumes: dashboard/package.json's existing "lint" and "build" npm scripts (already present in the scaffold: `eslint .` and `vite build`).
- Produces: two independently-triggered CI workflows, for Task 7 to confirm both are green on the opened PR.

- [ ] Step 1: Write .github/workflows/dashboard-ci.yml

Write this exact content:

    name: Dashboard CI

    on:
      push:
        paths:
          - 'dashboard/**'
      pull_request:
        paths:
          - 'dashboard/**'

    permissions:
      contents: read

    jobs:
      build:
        runs-on: ubuntu-latest
        defaults:
          run:
            working-directory: dashboard
        steps:
          - uses: actions/checkout@v4

          - uses: actions/setup-node@v4
            with:
              node-version: '20'
              cache: npm
              cache-dependency-path: dashboard/package-lock.json

          - name: Install dependencies
            run: npm ci

          - name: Lint
            run: npm run lint

          - name: Build
            run: npm run build

- [ ] Step 2: Add matching path filters to .github/workflows/ci.yml

Read the current file first (`cat .github/workflows/ci.yml`) to see its exact current `on:` block (it currently has no paths filters - just bare `push:` and `pull_request:` keys). Replace only that `on:` block with:

    on:
      push:
        paths:
          - 'pipelines/**'
          - 'evaluation/**'
          - 'data/**'
          - 'tests/**'
          - 'requirements-dev.txt'
      pull_request:
        paths:
          - 'pipelines/**'
          - 'evaluation/**'
          - 'data/**'
          - 'tests/**'
          - 'requirements-dev.txt'

Do not change anything else in this file (the jobs/steps below the `on:` block stay exactly as they are).

- [ ] Step 3: Verify both files are valid YAML and scoped correctly

Run: python -c "import yaml; yaml.safe_load(open('.github/workflows/dashboard-ci.yml')); yaml.safe_load(open('.github/workflows/ci.yml')); print('both valid YAML')"
(If PyYAML isn't installed, `pip install pyyaml` first, or visually double check indentation instead and note that in your report.)

Run: git diff --stat
Expected: exactly two files changed - .github/workflows/dashboard-ci.yml (new) and .github/workflows/ci.yml (its on: block only).

- [ ] Step 4: Commit

Run:
git add .github/workflows/dashboard-ci.yml .github/workflows/ci.yml
git commit -m "Add path-filtered dashboard CI; scope existing CI to Python paths"

---

### Task 6: Cross-link dashboard/ in the root README's repository layout

Files:
- Modify: README.md (the "Repository layout" fenced tree diagram only)

Interfaces:
- Consumes: nothing new - this is a pure documentation edit.
- Produces: an updated root README for Task 7's final diff check.

- [ ] Step 1: Read the current tree exactly as it is

Run: sed -n '96,110p' README.md (or open the file and look at the "## Repository layout" section)

You will see a fenced code block listing prompts/, profiles/, skills/, grounding/, pipelines/, evaluation/, data/, and mcp/, each using box-drawing tree characters (vertical bar + dash for middle entries, and an L-shaped corner character for the last entry). The `data/` line currently uses a middle-branch connector; the `mcp/` line currently uses the corner (last-entry) connector.

- [ ] Step 2: Insert a new dashboard/ line and fix the connector

Insert a new line for dashboard/ immediately after the data/ line and before the mcp/ line, using the same middle-branch connector style as the other non-last entries (matching the exact character the data/ and pipelines/ lines already use), with the description "Model tuning dashboard (React + Vite, client-side)" aligned the same way the other descriptions are. The mcp/ line - now no longer last - keeps its own connector as-is only if a new entry doesn't come after it; since dashboard/ is being inserted before it, mcp/ remains the true last entry and keeps its existing corner connector unchanged. (In other words: you are only adding one new line with a middle-branch connector - you do not need to change any existing line's connector, since mcp/ stays last.)

- [ ] Step 3: Verify the edit

Run: git diff README.md
Expected: exactly one new line added (pure addition, matching the style/alignment of the surrounding lines), zero lines removed, zero lines modified elsewhere in the file.

- [ ] Step 4: Commit

Run:
git add README.md
git commit -m "Cross-link dashboard/ from the root repository layout"

---

### Task 7: Verify everything end-to-end, then push and open the PR

Files:
- Create: pr-body.md (temporary, repo root - used only to pass the PR description to gh, not committed)
- None else created/modified - verification and delivery only.

Interfaces:
- Consumes: full branch state from Tasks 1-6.
- Produces: pushed branch add-rtm-dashboard and an open draft PR - the plan's final deliverable.

- [ ] Step 1: Re-run the dashboard build from a clean install

Run:
cd dashboard
npm ci
npm run lint
npm run build
cd ..

npm ci always removes any existing node_modules before installing from package-lock.json, so this is already a clean, reproducible install - the same one the CI workflow will run.

Expected: npm ci succeeds, lint reports no errors, build exits 0.

- [ ] Step 2: Re-confirm the toolkit's own Python suite still passes (sanity check that nothing in Tasks 1-6 touched it)

Run: python -m pytest -q tests
Expected: same pass count as before this branch existed (25 passed, per the prior session's work on this same repo) - if the count differs, stop and investigate before proceeding; do not push.

- [ ] Step 3: Confirm the whole branch's diff is scoped to the intended files only

Run: git diff f431659 HEAD --stat -- . ':!dashboard' ':!README.md' ':!.github/workflows/dashboard-ci.yml' ':!.github/workflows/ci.yml' ':!docs/superpowers'
Expected: empty output - nothing leaked outside dashboard/, the README cross-link, the two workflow files, and this plan's own docs.

- [ ] Step 4: Write the PR body to a file

Write this exact content to pr-body.md at the repo root:

    ## Summary
    - Subtree-merges overjoyde/rtm-model-tuning-dashboard's full history into dashboard/, preserving its commit(s).
    - Adds a real first view: per-detector score-threshold slider with live precision/recall/F1 recomputed in-browser against the toolkit's synthetic ground truth, plus a hand-rolled SVG chart and the configured floor as a reference line. No detector logic changed - this tunes the alert-prioritization score threshold over already-flagged entities.
    - New dashboard/scripts/export_tuning_snapshot.py regenerates the checked-in dashboard/src/data/tuning-snapshot.json sample from the toolkit's existing synthetic generator, pipeline, and ground truth.
    - Adds path-filtered dashboard-ci.yml (npm ci/lint/build) and scopes the existing ci.yml to its own Python paths, so each workflow only runs on its own project's changes.
    - Cross-links dashboard/ from the root README's repository layout. No dependency unification between the toolkit's pip stack and the dashboard's npm stack (see docs/superpowers/specs/2026-07-20-rtm-dashboard-merge-design.md).

    ## Test plan
    - [x] dashboard: clean npm ci + npm run lint + npm run build all pass
    - [x] toolkit's own pytest suite (tests/) unaffected, same pass count as before this branch
    - [x] Diff outside dashboard/, README.md, and the two workflow files is empty
    - [ ] dashboard-ci.yml passes on this PR in GitHub Actions
    - [ ] ci.yml passes on this PR in GitHub Actions (and does NOT run on dashboard-only pushes going forward)

    Generated with Claude Code

- [ ] Step 5: Push the branch

Run: git push -u origin add-rtm-dashboard
Expected: branch created on overjoyde/bear-fincrime-toolkit-2.0.

- [ ] Step 6: Open the draft PR

Run:
gh pr create --repo overjoyde/bear-fincrime-toolkit-2.0 --draft --base main --head add-rtm-dashboard --title "Fold rtm-model-tuning-dashboard into dashboard/, add live tuning view" --body-file pr-body.md

Expected: prints the PR URL.

- [ ] Step 7: Clean up the temporary PR body file

Run: rm pr-body.md
Expected: no output; `git status` shows no trace of pr-body.md (it was never committed, so this is just local cleanup).

---

## Self-Review Notes

- Spec coverage: design section 1 (folder layout) maps to Task 1; section 2 (history) maps to Task 1; section 3 (snapshot schema/pipeline) maps to Task 2; section 4 (CI) maps to Task 5; section 5 (React structure) maps to Task 4 (metrics.js split out into its own Task 3 since it has an independent, isolable interface and is a natural spec-vs-quality gate on its own); section 6 (docs cross-linking) maps to Task 6; section 7 (out-of-scope) is enforced by Task 7 Step 3's diff check; Testing/Delivery maps to Task 7.
- Correction from the spec: spec section 6's prose said the previous last entry's connector "changes from a corner to a middle branch" to accommodate the new line, but its own code sample showed dashboard/ inserted BEFORE mcp/ (so mcp/ stays last and keeps its corner connector unchanged - only a new middle-branch line for dashboard/ is added). Task 6 above follows the code sample, which is the internally-correct version - the prose sentence in the spec was simply imprecise given where the code sample actually placed the new line. No behavior needs to change from what was demoed in the spec; only that one sentence of the spec's prose description was inconsistent with its own example.
- No placeholders: every step has literal commands, code, or YAML to run or write.
- Type/name consistency: computeMetricsAtThreshold and metricsCurve are exported from metrics.js in Task 3 exactly as imported in DetectorTuningPanel.jsx in Task 4; the tuning-snapshot.json shape produced in Task 2 (flagged/ground_truth_positive_ids/ground_truth_positive_count/floor) is consumed identically in Task 3's fixture and Task 4's App.jsx/DetectorTuningPanel.jsx.
